import React, { useEffect } from 'react'
import { useDispatch } from 'react-redux'
import { Actions, PayloadType } from '../../store/actions'

type PropsType = {
	props: PayloadType[]
}

export const getNotes = (notes: PayloadType[]) => {
	
}
